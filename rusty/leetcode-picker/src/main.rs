use leetcode_picker::*;

fn main() -> Result<(), String> {
    let url = "https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/";
    //let rep = get_quiz_by_name(url)?;
    //dbg!(&rep.text());
    //parse_response_body(rep);
    Ok(())
}
